# lukedhlee.github.io

Personal website for Donghyun Lee (Luke), built with Jekyll and hosted on GitHub Pages.

## Local Preview

```bash
bundle install
bundle exec jekyll serve
```

Open `http://127.0.0.1:4000`.

If Ruby/Bundler is not set up, GitHub Pages will still build the site after pushing to `main`.
